/**
 * Placeholder. v1 used to render a fixed-position banner with the current
 * step title; that was replaced by inline DemoAIIndicator. Kept as a no-op
 * so future tenants can reintroduce a banner without changing the App tree.
 */
export default function DemoStepBanner() {
  return null;
}
